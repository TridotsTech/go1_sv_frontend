// import { Tabs, TabList, Tab } from '@chakra-ui/react'
export default function CategoryTabs({ data,filterData}) {

    return (
        <>
            {/* <Tabs>
                <TabList>
                    {data.map(res=>{
                        return(
                            <Tab onClick={()=> filterData(res)} key={res.name}>{res.name}</Tab>
                        )
                    })}
                </TabList>
            </Tabs> */}

        </>
    )
}